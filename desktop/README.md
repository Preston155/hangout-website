# Discord Remake — Desktop App

**Native Windows app** using WebView2 (.NET). No Electron.

```bash
npm run build:desktop      # builds desktop-native/
npm run build:httpdocs     # includes downloads/Discord-Remake-Setup.exe
```

See `desktop-native/README.md` for details.
