import {NextResponse} from 'next/server';import {prisma} from '@/lib/prisma';export async function POST(req:Request){const body=await req.json();const report=await prisma.report.create({data:{profileId:body.profileId,reportedUserId:body.reportedUserId,reason:String(body.reason??'Other').slice(0,80),details:String(body.details??'').slice(0,1200)}});return NextResponse.json({report})}
