import Link from 'next/link';export type PublicProfile={username:string;slug:string;headline?:string|null;bio?:string|null;avatarUrl?:string|null;bannerUrl?:string|null;views:number};export function ProfileCard({profile}:{profile:PublicProfile}){return <article className="glass overflow-hidden rounded-[2rem]"><div className="h-36 bg-gradient-to-br from-neon/30 via-hot/20 to-aqua/20" style={profile.bannerUrl?{backgroundImage:`url(${profile.bannerUrl})`,backgroundSize:'cover',backgroundPosition:'center'}:{}}/><div className="p-6"><img src={profile.avatarUrl||'/favicon.svg'} alt="" className="-mt-16 mb-3 h-24 w-24 rounded-3xl border border-white/20 bg-ink object-cover shadow-glow"/><h2 className="text-2xl font-black">@{profile.username}</h2><p className="mt-1 text-white/70">{profile.headline||'PrestonHQ creator'}</p><p className="mt-4 line-clamp-3 text-sm text-white/60">{profile.bio}</p><div className="mt-5 flex items-center justify-between text-sm text-white/55"><span>👁 {profile.views.toLocaleString()} views</span><Link href={`/@${profile.slug}`} className="text-aqua">Open →</Link></div></div></article>}
